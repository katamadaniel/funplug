import React from 'react';
import { Typography } from '@mui/material';

const AdminEmails = () => {
  return (
    <div>
      <Typography variant="h4">Manage Emails</Typography>
      {/* Add content for managing emails */}
    </div>
  );
};

export default AdminEmails;
