import React from 'react';
import { Typography } from '@mui/material';

const AdminInvoices = () => {
  return (
    <div>
      <Typography variant="h4">Manage Invoices</Typography>
      {/* Add content for managing invoices */}
    </div>
  );
};

export default AdminInvoices;
